(()=>{'use strict';const q=(s,c=document)=>c.querySelector(s),qa=(s,c=document)=>[...c.querySelectorAll(s)];q('[data-year]')?.replaceChildren(String(new Date().getFullYear()));const toggle=q('.nav-toggle'),nav=q('#nav');if(toggle&&nav){const close=()=>{nav.classList.remove('open');toggle.setAttribute('aria-expanded','false')};toggle.addEventListener('click',()=>{const open=!nav.classList.contains('open');nav.classList.toggle('open',open);toggle.setAttribute('aria-expanded',String(open))});nav.addEventListener('click',close);document.addEventListener('keydown',e=>{if(e.key==='Escape')close()})}const io='IntersectionObserver'in window?new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');io.unobserve(entry.target)}}),{threshold:.12}):null;qa('.reveal').forEach(el=>io?io.observe(el):el.classList.add('visible'));const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;if(!reduced&&matchMedia('(pointer:fine)').matches){qa('.tilt').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(900px) rotateX(${-y*3}deg) rotateY(${x*4}deg)`});el.addEventListener('pointerleave',()=>el.style.transform='')})}const widget=q('[data-signal-widget]');if(widget){const projects=[{n:'01',name:'Deckler Engineering',href:'#deckler',color:'#f04e2e'},{n:'02',name:'Anime Social',href:'#anime',color:'#7a43d8'},{n:'03',name:'ERA Snacks',href:'#era',color:'#e7a83a'}];let i=0;const button=q('button',widget),num=q('button span',widget),name=q('strong',widget),link=q('a',widget);const cycle=()=>{i=(i+1)%projects.length;const p=projects[i];num.textContent=p.n;name.textContent=p.name;link.href=p.href;button.style.transform=`rotate(${i*120}deg)`;q('.signal-section').style.background=p.color};button.addEventListener('click',cycle);button.addEventListener('keydown',e=>{if(e.key==='ArrowRight'||e.key==='ArrowDown'){e.preventDefault();cycle()}})} })();
