(()=>{'use strict';const $=(s,c=document)=>c.querySelector(s),$$=(s,c=document)=>[...c.querySelectorAll(s)];$('[data-year]')?.replaceChildren(String(new Date().getFullYear()));const menu=$('.menu'),nav=$('#nav');if(menu&&nav){const close=()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false')};menu.addEventListener('click',()=>{const open=!nav.classList.contains('open');nav.classList.toggle('open',open);menu.setAttribute('aria-expanded',String(open))});nav.addEventListener('click',close);document.addEventListener('keydown',e=>{if(e.key==='Escape')close()})}const observer='IntersectionObserver'in window?new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12}):null;$$('.reveal').forEach(el=>observer?observer.observe(el):el.classList.add('visible'));const clock=$('[data-clock]');if(clock){const tick=()=>clock.textContent=new Intl.DateTimeFormat('en-GB',{timeZone:'Africa/Lagos',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false}).format(new Date());tick();setInterval(tick,1000)}const index=$('[data-project-index]'),preview=$('.hover-preview');if(index&&preview&&matchMedia('(hover:hover) and (pointer:fine)').matches&&!matchMedia('(prefers-reduced-motion: reduce)').matches){const img=$('img',preview);let tx=innerWidth*.72,ty=innerHeight*.55,x=tx,y=ty,raf;const move=()=>{x+=(tx-x)*.14;y+=(ty-y)*.14;preview.style.left=x+'px';preview.style.top=y+'px';raf=requestAnimationFrame(move)};move();index.addEventListener('pointermove',e=>{tx=Math.min(e.clientX+90,innerWidth-preview.offsetWidth*.5-20);ty=Math.min(Math.max(e.clientY,preview.offsetHeight*.5+20),innerHeight-preview.offsetHeight*.5-20)});$$('.project-row',index).forEach(row=>{row.addEventListener('pointerenter',()=>{img.src=row.dataset.preview;preview.classList.add('visible');index.style.background=row.dataset.tone});row.addEventListener('pointerleave',()=>preview.classList.remove('visible'));row.addEventListener('focus',()=>{img.src=row.dataset.preview;preview.classList.add('visible')});row.addEventListener('blur',()=>preview.classList.remove('visible'))});addEventListener('pagehide',()=>cancelAnimationFrame(raf))}const form=$('#quoteForm');form?.addEventListener('submit',e=>{e.preventDefault();if(!form.reportValidity())return;const data=new FormData(form),msg=`Hello Victor, my name is ${data.get('name')}. I need help with: ${data.get('service')}.%0A%0AProject overview: ${data.get('message')}`;location.href=`https://wa.me/2348082413412?text=${encodeURIComponent(decodeURIComponent(msg))}`})})();
